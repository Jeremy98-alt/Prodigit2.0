config:
  target: "https://90etzbh1hf.execute-api.us-east-1.amazonaws.com/dev" # change this every time
  phases:
    - duration: 120
      arrivalRate: 3
      name: Warm up
    - duration: 300
      arrivalRate: 5
      rampTo: 50
      name: Ramp up load
    - duration: 600
      arrivalRate: 10
      name: Sustained load

scenarios:
  - name: "testPrenot"
    flow:
      - get:
          url: "/bookingseats"

# First of all, is important to install artillery, here there is the official guide: https://artillery.io/docs/guides/getting-started/installing-artillery.html
## IMPORTANT to have the last version of npm, node.js component to install every dependency you want
